Required Software:
	 - autotools (only for developers)
	 - BLAS
	 - LAPACK library
Optional Software:
	 - gauopen
	 - Gaussian 16 or higher
To build:
   chmod 755 mqc_install
   ./mqc_install
   {Answer questions}
